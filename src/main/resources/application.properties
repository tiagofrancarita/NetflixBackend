spring.application.name=NetflixClone

# Define o perfil ativo
spring.profiles.active=dev

# Contexto base da aplica��o (ex: localhost:8080/netflix)
server.servlet.context-path=/netflix
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# Timezone
spring.jackson.time-zone=America/Sao_Paulo

# Internacionaliza��o (se desejar no futuro)
spring.messages.basename=messages

spring.security.user.name=tiago
spring.security.user.password=123456

# ================= JWT CONFIG =================
# Chave secreta usada para assinar o token JWT (ex: HMAC256)
jwt.secret=7b9e2f23d5a7429f8b1c3d6a9f1e0b7c

# Tempo de expiração do token em milissegundos (900000 = 15 minutos)
jwt.expiration=900000
#jwt.expiration=10000
#10 segundos

# Tipo do token retornado (geralmente "Bearer")
jwt.token-type=Bearer

# Chave de criptografia para dados sensíveis (opcional, para payload ou claims adicionais)
jwt.encrypt-key=Th!sIs@V3ry$3cur3EncryptKey456

# Chave de assinatura separada (caso queira usar uma para assinar e outra para criptografar dados extras)
jwt.sign-key=R3h9!@#DqLp78Kj3VmNxQzRw5Eb2FcYn

springdoc.show-actuator=true